extends layout.pug

block content
    h2.page-header Register
    p Please register using the form below
    if errors
        each error, i in errors
            div.alert.alert-danger #{error.msg}
    form(method="post" action="/users/register" enctype="multipart/form-data")
        .form-group
        label Name
        input.form-control(type="text" name="name" placeholder="enter name")
        label Email
        input.form-control(type="text" name="email" placeholder="enter email")
        label Username
        input.form-control(type="text" name="username" placeholder="enter username")
        label Password
        input.form-control(type="text" name="password" placeholder="enter password")
        label Confirm Password
        input.form-control(type="text" name="password2" placeholder="enter password")
        label Profile image
        input.form-control(type="file" name="profileimage")
        input.btn.btn-primary(type="submit" name="submit" value="Register")